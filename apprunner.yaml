version: 1.0
runtime: PYTHON_3
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  command: gunicorn --workers 4 --bind :$PORT run:app
  network:
    port: 5000
  runtime_environment_variables:
    PORT: 5000
    AWS_REGION_NAME: us-east-1
